@model IEnumerable<_50331_TUHTIAEV.Models.MenuItem>

<ul>
    @foreach (var item in Model)
    {
        <li>
            @Html.ActionLink(item.Name, item.Action, item.Controller)
        </li>
    }
</ul>
